{
    "compilerOptions": { // absolute path using VScode
      "baseUrl": ".",
      "paths": {
        "~/*": [
          "./*"
        ],
        "@/*": [ 
          "./src/*"
        ],
      }
    },
    "exclude": [
      "node_modules", // exclude library, build
      "dist"
    ]
  }